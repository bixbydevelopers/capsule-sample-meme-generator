capsule {
	id (example.memeGenerator)
	version (0.1.0)
	format (3)
	targets {
		target (bixby-mobile-en-US)
	}
	capsule-imports {
		import (viv.punchoutToApp) { as (punchout) version(1.0.17)}
	}
  store-sections {
    section (GamesAndFun)
  }
	runtime-version (2)
	store-countries {
		all
	}
}
